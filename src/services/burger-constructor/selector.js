// import { createSelector } from '@reduxjs/toolkit';

// export const getConstructIngredients = (store) => store.construct.consruct.ingredients;
// export const getConstructBun = (store) => store.construct.consruct.bun;

// // export const hasOneTasks = createSelector(
// //   (state) => state.tasks.tasks,
// //   (tasks) => tasks.filter((task) => task.content.includes("1"))
// // );

// export const costructIngredientsCounter = createSelector(
//     (state) => state.construct.consruct.ingredients,
//     (ingredients) => {
//         let counterObj = {};


//     }
//   );


